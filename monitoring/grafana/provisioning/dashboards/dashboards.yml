apiVersion: 1

providers:
  - name: MedAssist
    orgId: 1
    folder: MedAssist
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards